<UserControl x:Class="WPFApp.FormAround"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WPFApp"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="450">

    <Grid x:Name="MainGrid">
        <Grid.RowDefinitions>
            <RowDefinition Height="8*" MinHeight="40"/>
            <RowDefinition Height="100*"/>
            <RowDefinition Height="5*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="10*" MinWidth="70"/>
            <ColumnDefinition Width="100*"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>

        <Grid x:Name="HeaderGrid" Grid.Row="0" Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="70*"/>
                <RowDefinition Height="100*"/>
            </Grid.RowDefinitions>

            <Label Grid.Row="0" Name="groupLabel" Content="{Binding LabelParentGroup}"/>
            <Label Grid.Row="1" Name="currentLabel" Content="{Binding LabelCurrent}"/>
        </Grid>

        <Grid x:Name="ButtonsLeftGrid" Grid.Row="1" Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="20*"/>
                <RowDefinition Height="80*"/>
            </Grid.RowDefinitions>

            <Button Grid.Row="0" Name="HomeButton" HorizontalAlignment="Stretch"
                    VerticalAlignment="Stretch" Command="{Binding HomePageCommand}"
                    Style="{StaticResource MaterialDesignFloatingActionDarkButton}">
                <materialDesign:PackIcon
                    Kind="Home"
                    Height="24"
                    Width="24" />
            </Button>

            <Button Grid.Row="1" Name="BackButton" HorizontalAlignment="Stretch"
                    VerticalAlignment="Stretch"
                    Style="{StaticResource MaterialDesignFloatingActionDarkButton}">
                <materialDesign:PackIcon
                    Kind="ArrowBack"
                    Height="24"
                    Width="24" />
            </Button> 
        </Grid>

        <ScrollViewer VerticalScrollBarVisibility="Auto" Name="panelStk" Grid.Row="1" 
                      Grid.Column="1" VerticalContentAlignment="Center"/>
    </Grid>
</UserControl>
